# Security Policy

Report vulnerabilities privately to security@unsold.group.

Do not include secrets in issues, discussions, logs, or screenshots. `mcp-local-relay` intentionally keeps credentials out of LaunchAgent plists and client MCP config; secrets should be provided through private env files or a local secret manager.
